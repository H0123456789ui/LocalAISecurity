# LocalAISecurity — Python 运行时依赖
# Python层负责Tkinter UI + PyTorch推理(fallback)
# C++层负责INT4量化推理引擎 + Windows内核监控

# 核心运行时（必须）
numpy>=1.24.0
Pillow>=9.0.0
pystray>=0.19.0
psutil>=5.9.0

# 可选：PyTorch推理引擎（未安装时自动降级为规则引擎）
# torch>=2.0.0

# 可选：WMI实时进程监控（未安装时自动降级为psutil轮询）
# wmi>=1.5.0

# 可选：模型导出工具（训练时使用，运行时不需要）
# onnx>=1.14.0
# onnxruntime>=1.15.0

# 测试
pytest>=7.0.0
